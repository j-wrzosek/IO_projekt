name: Build and Deploy Spring Boot to Azure

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Make Maven wrapper executable
        run: chmod +x mvnw

      - name: Build with Maven
        run: ./mvnw clean package -DskipTests

      - name: List built files (debug)
        run: ls -lh target

      - name: Create ZIP package
        run: |
          mkdir deploy
          cp target/app.jar deploy/app.jar
          cd deploy
          zip -r ../deployment.zip .

      - name: Check ZIP contents (debug)
        run: unzip -l deployment.zip

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: springboot-linux-demo              # 🟢 Twoja aplikacja
          slot-name: production
          publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}  # 🟢 Secret musi być dodany
          package: deployment.zip
